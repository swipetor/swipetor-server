@inject PhotoUrlBuilderSvc PhotoUrlBuilderSvc
@using WebLibServer.Photos
@model SwipetorApp.Models.ViewModels.PhotoPartialViewModel

@{
    Layout = null;
}

@if (Model.Photo != null)
{
    <img src="@PhotoUrlBuilderSvc.GetFullUrl(Model.Photo, Model.Size)" alt="Photo"/>
}